export enum ROUTE {
  HOME = "/",
  TRENDS = "trends",
  FAVORITES = "favorites",
  SETTINGS = "settings",
  LOGIN = "login",
  REGISTRATION = "registration",
  MOVIE = "movie/:id",
  RESET_PASSWORD = "password/reset",
  SEARCH = "search/:name",
}
